import BaseModule from '@/store/base-module';
import QuestionService from "@/services/QuestionService";

var base = new BaseModule(QuestionService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};