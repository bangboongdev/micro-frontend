import BaseModule from '@/store/base-module';
import CompanyService from "@/services/CompanyService";

var base = new BaseModule(CompanyService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};