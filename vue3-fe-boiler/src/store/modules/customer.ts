import BaseModule from '@/store/base-module';
import CustomerService from "@/services/CustomerService";

var base = new BaseModule(CustomerService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};