import BaseModule from '@/store/base-module';
import PackageService from "@/services/PackageService";

var base = new BaseModule(PackageService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};