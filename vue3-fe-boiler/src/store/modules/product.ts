import BaseModule from '@/store/base-module';
import ProductService from "@/services/ProductService";

var base = new BaseModule(ProductService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};