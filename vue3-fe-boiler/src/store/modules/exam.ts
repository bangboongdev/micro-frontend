import BaseModule from '@/store/base-module';
import ExamService from "@/services/ExamService";

var base = new BaseModule(ExamService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};