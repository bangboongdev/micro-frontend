version: "3"
services:
  frontend:
    build: .
    image: vue3
    container_name: vue3
    restart: always
    ports:
      - 14442:9998
    # command: "npm run prod"
    # tty để theo dõi % build 
    # tty: true
    network_mode: bridge
   
